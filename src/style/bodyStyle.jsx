import color from '../constants/palette';
const styles = (theme) => ({ 
    dateslider:{
        backgroundColor:color.datesliderBackground
    },
    chart:{
        backgroundColor:color.chartBackground
    }
});

export default styles